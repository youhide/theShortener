{
  "name": "theshortener",
  "version": "0.0.5",
  "description": "String Shortener",
  "main": "index.js",
  "dependencies": {
    "nan": "^2.0.0"
  },
  "scripts": {
    "build": "node-gyp configure && node-gyp build && cp ./build/Release/shortener.node ./",
    "test": "ava"
  },
  "devDependencies": {
    "ava": "^0.19.1"
  }
}
